import './globals.css';
export const metadata={title:'GrantWitness — Public Milestone Ledger',description:'Validator-backed grant milestone accountability on GenLayer.'};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
